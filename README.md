# change to React version

A simple React + Redux implementation.

## Running

First `yarn install` to grab all the necessary dependencies.

Then run `npm start` and open <localhost:7878> in your browser.

